{
  "name": "piano-reading-music",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build -w web"
  },
  "devDependencies": {
    "eslint": "^8",
    "typescript": "^5"
  }
}